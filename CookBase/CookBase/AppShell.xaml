<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="CookBase.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:CookBase"
    Title="CookBase"
    FlyoutBehavior="Disabled"
    Shell.NavBarIsVisible="False">
    <FlyoutItem Title="Strona główna" Route="main">
        <ShellContent ContentTemplate="{DataTemplate local:MainPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Produkty" Route="products">
        <ShellContent ContentTemplate="{DataTemplate local:ProductsPage}" />
    </FlyoutItem>

    <FlyoutItem Title="Kategorie" Route="categories">
        <ShellContent ContentTemplate="{DataTemplate local:CategoriesPage}" />
    </FlyoutItem>

</Shell>
